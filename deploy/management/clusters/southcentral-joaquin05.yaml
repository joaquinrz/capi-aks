apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: joaquin05-aks-helm-release
  namespace: default
spec:
  chart:
    spec:
      chart: ./capi/charts/aks
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  interval: 10m
  values:
    name: joaquin05-aks
    clusterResourceGroup: joaquin-cluster-05
    controlAgentPoolName: claspoolfive
    workerAgentPoolName: wraspoolfive
